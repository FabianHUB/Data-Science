import pandas as pd

customerID = int(float(input('Waiting for input: ')))  # example: 101

df = pd.read_csv("icecream.csv")

### ADD YOUR CODE BELOW

df_filtered = df[df['ID'] == customerID]

order_cell = df_filtered['scoop_1'].iloc[0]
order = str(order_cell)

age_cell = df_filtered['age'].iloc[0]
customer_age = int(age_cell)

# customer_age = 999
# order = 999

### ADD YOUR CODE ABOVE

print(f"customer age is {customer_age} and their first scoop flavour is {order}.")